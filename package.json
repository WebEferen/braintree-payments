{
  "name": "braintree-payments",
  "version": "2.4.1",
  "description": "Typescript Braintree Payments",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "postinstall": "npm run build",
    "tslint": "./node_modules/.bin/tslint -p tsconfig.json || exit 0",
    "mocha": "./node_modules/.bin/mocha --timeout 5000 --reporter spec || exit 0",
    "documentation": "./node_modules/.bin/typedoc --out docs --exclude node_modules",
    "nyc": "./node_modules/.bin/nyc --reporter=html --reporter=text",
    "clear": "./node_modules/.bin/rimraf ./dist",
    "build": "./node_modules/.bin/tsc",
    "test": "npm run clear && npm run build && npm run mocha",
    "test-coverage": "npm run nyc npm run test || exit 0"
  },
  "engines": {
    "node": "8.9.4"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/WebEferen/braintree-payments.git"
  },
  "author": "Mike Makowski",
  "license": "ISC",
  "bugs": {
    "url": "https://gitlab.com/WebEferen/braintree-payments/issues"
  },
  "homepage": "https://gitlab.com/WebEferen/braintree-payments#README",
  "dependencies": {
    "await-to-js": "^2.0.1",
    "braintree": "^2.12.0",
    "lodash": "^4.17.10",
    "@types/lodash": "^4.14.116",
    "@types/node": "^10.7.1"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typedoc": "^0.12.0",
    "typescript": "^3.0.1"
  }
}
